# NexusD Demo Cluster Configuration

# Cluster Settings
CLUSTER_NAME=demo-cluster
CLUSTER_NODE_COUNT=3

# Network Configuration
BASE_APP_PORT=5672
BASE_MESH_PORT=5680
BASE_DISCOVERY_PORT=5690

# Multicast Discovery
MULTICAST_GROUP=239.255.77.77
MULTICAST_PORT=5670

# Logging
LOG_LEVEL=info
LOG_DIR=./logs

# Data Directory
DATA_DIR=./data

# Daemon Binary Path (relative to demo folder)
NEXUSD_BIN=../../build/nexusd

# Timeouts (in seconds)
STARTUP_DELAY=1
SHUTDOWN_TIMEOUT=5

# Optional: Node-specific overrides
# NODE_1_APP_PORT=5672
# NODE_2_APP_PORT=5673
# NODE_3_APP_PORT=5674
